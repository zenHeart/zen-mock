module.exports = {
    code:/\d{6}/,//生成符合正则的 6 字符串,
    msg:"@sentense(3,5)",//生成 3-5 个单词组成的句子,
    data:{
        'record|1-3':[ //生成一个长度为 1-3 的数组
            {
                id:/\w{32}/ //生成符合正则的 32 位字符串
            }
        ]
    }
}